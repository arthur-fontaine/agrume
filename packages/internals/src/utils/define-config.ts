import type { CliOptions } from '@agrume/types'

/**
 * An helper to define the configuration.
 * @param {CliOptions} config The configuration.
 * @returns {CliOptions} The configuration.
 */
export const defineConfig = (config: CliOptions) => config
