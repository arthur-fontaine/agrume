{
  "name": "@agrume/internals",
  "version": "3.4.0",
  "main": "./dist/internals.js",
  "module": "./dist/internals.mjs",
  "types": "./dist/internals.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/internals.ts --dts --format esm,cjs --minify --sourcemap inline",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@agrume/types": "workspace:*",
    "c12": "^1.11.2",
    "defu": "^6.1.4",
    "node-machine-id": "^1.1.12"
  },
  "devDependencies": {
    "@agrume/tunnel": "workspace:*",
    "@types/node": "^20",
    "@types/prettier": "^3.0.0",
    "prettier": "^3.3.3",
    "tsup": "^8.2.4"
  }
}
